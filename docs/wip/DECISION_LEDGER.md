# Outstanding-Decision Ledger

**Convention**: this is the single cross-cutting view of every decision pending the owner and every open fix ruling. Item numbers are stable — resolved items keep their number with a `RESOLVED` status rather than being deleted. Each item's source of truth is its cited document; when an item moves, update both. Grouped by kind.

## A. Bug-fix direction rulings

1. **Decorrelation mishandles predicate-only `:in` symbols** — RESOLVED (2026-07-20). Correlation parameters are classified before the rewrite fires (data-bound / equality-bound translation / decline with `algebra/decorrelate-skip`); the correlation equality becomes the join condition; `LateralJoin` carries the call-site argument list verbatim so declined subqueries round-trip losslessly. The corrected rewrite exposed two independent engine defects, fixed under their own entries: the planner's readiness-restricted subquery deferral gate and the executor's expression-binding unification. All seven guards green both modes. `docs/bugs/resolved/BUG_DECORRELATION_PREDICATE_ONLY_INPUT_SYMBOLS.md`, `resolved/BUG_UNCORRELATED_SUBQUERY_SCHEDULES_BEFORE_BINDING_PROVIDERS.md`, `resolved/BUG_EXPRESSION_BINDING_OVERWRITES_BOUND_VARIABLE.md`.
2. **Algebra bridge compiles in source order (R4)** — RESOLVED (2026-07-20), direction ruled: pre-order by `ScopeOf` readiness. Readiness single-homed as `query.ClauseBlockers`/`query.ClauseReady` (+ `query.CountProviders`), consumed by both the planner's `clauseSelectable` and the bridge's `orderClausesForCompile`; `compileClausesFrom` orders every clause list (top level, OR branches, NOT bodies) before folding. Same change fixed `optimizeAlgebra` and the decorrelation inner compiles stripping `:in` from the queries they compile. Pin `TestClauseOrderIndependenceForNot` green all four legs; two invalid-query rejection tests now reject at the ordering gate, still naming the unbindable symbol. `docs/bugs/resolved/BUG_ALGEBRA_BRIDGE_COMPILES_IN_SOURCE_ORDER.md`.
3. **not-join header validation only on algebra path** — RESOLVED (2026-07-20), closed by item 8: header completeness validates at the shared boundary via `NotJoinClause.Validate()` under `query.ValidateStaticClauseShapes`. `docs/bugs/resolved/BUG_NOTJOIN_HEADER_VALIDATION_ONLY_ON_ALGEBRA_PATH.md`.
4. **Bridge rejects single-branch or-join in NOT bodies** — RESOLVED (2026-07-20). Union IR arity relaxed to ≥ 1 (clause-level minimums remain language rules at Validate); decompile round-trip preserved. `docs/bugs/resolved/BUG_ALGEBRA_NOT_REJECTS_SINGLE_BRANCH_ORJOIN.md`.
5. **Bridge rejects or-default as first clause** — RESOLVED (2026-07-20). The lowering extends to a unit-relation outer group (childless empty `Project` = join identity); one code path, no special case. `docs/bugs/resolved/BUG_ALGEBRA_ORDEFAULT_FIRST_CLAUSE_REJECTED.md`.
6. **AsOf/History drop planner options** — RESOLVED (2026-07-20). Temporal handles inherit `plannerOptions` + `parseCache`; the 18-field disposition table is enforced by a reflection classification guard so new `Database` fields must declare their temporal-handle treatment. `docs/bugs/resolved/BUG_TEMPORAL_HANDLES_DROP_PLANNER_OPTIONS.md`, `resolved/BUG_TEMPORAL_DATABASE_HANDLES_ARE_SHALLOW.md`.
7. **Pre-existing, distinct**:
   - `docs/bugs/resolved/BUG_SUBQUERY_MULTIPLE_INPUTS.md` — RESOLVED (2026-07-20) as a misdiagnosis: the engine's multi-input constant forwarding was never broken. The reproducer's date constant was a bare EDN string matched against `time.Time` datoms; type-strict matching correctly returns empty. With `#inst` the test is green both modes with no engine change. Skip removed; `TestSubqueryWithTwoInputs` now pins the forwarding (variable + `#inst` constant), alongside `MultipleInputsWithDatabase` (variable + float constant).
   - `docs/bugs/resolved/BUG_STREAMING_RELATION_TRANSFORMS_SHARE_RAW_ITERATOR.md` — RESOLVED (2026-07-20 triage), overtaken by intervening work: `StreamingRelation.Filter` went with the Filter-system deletion; `FilterWithPredicate`/`EvaluateFunction` now compose from the guarded `r.Iterator()`; the report's requested Materialize-then-transform replay test exists verbatim (`TestStreamingFilterUsesRelationIteratorAndBuildsReplayCache`) alongside the deferred-error propagation suite.
   - `docs/bugs/BUG_WASM_STORAGE_GC_BAD_POINTER_CRASH.md` — upstream (golang/go#80472); CI rerun policy in place; tracking only unless posture changes.
   - `docs/bugs/resolved/BUG_MISSING_ON_LOOKUPLESS_MATCHER_SILENTLY_EMPTY.md` — RESOLVED (2026-07-20, same day), both defects fixed at their invariants after the owner ruled the most-correct derivation: (B) capability lies — `AnnotatedMatcher.LookupAttribute` and both `SourceRouter.LookupAttribute` arms error on absent capability ("capability absent" is never a value in the answer domain; data-answer vs best-effort line stated at the wrappers); (A) laundering — the eager producer family (`filterWithPredicateAndLookup`, `evaluateExpressionWithLookup`, `thetaJoinPair`/`WithPredicate`, `crossJoinWithExpression`) returns errors in-band (the deferred convention is exclusively for lazily-discovered streaming errors), and the irreducible errored-empty remainder is probed by the new single home `executor.EmptyRelationError` at every emptiness-branching consumer (both matchers' empty-binding fallbacks, relation-input entry, mock fixture; hash join verified already-correct and pinned; strategy arm verified safe via the pinned `matchWithoutIteratorReuse` propagation). The committed-red reproducer is green on all four legs; gate fully green, native + wasm, zero red. Discovery context preserved in the doc (initially mis-triaged and retracted — restored by owner ruling; see `feedback_evidence_never_downgraded`).
   - Stale-triage DONE (2026-07-20), both moved to `resolved/` with triage notes: `CONDITIONAL_AGGREGATE_STREAMING_DEPENDENCY.md` is OBSOLETE — its reproducers, cited source files, and two of its four cited options (`EnableConditionalAggregateRewriting`, `EnableIteratorComposition`) are gone; the failing streaming-disabled configuration is unrepresentable. `SUBQUERY_POSITIONAL_MAPPING_ISSUE.md` is RESOLVED — its own long-term proposal (true positional mapping including explicit `$`, loud arity validation) is the current implementation in `datalog/executor/subquery.go`, with the tests it requested in tree.

## B. Design ratifications

8. **Boundary-validation principle** — RATIFIED AND LANDED (2026-07-20). Static properties of query text validate once at the shared user boundary (parser / qb / executor entry, pre-planning so both modes agree), never first-detected in mode machinery. First applications: subquery binding-arity `Validate()`, not-join header completeness, or-default-join header (`query.ValidateStaticClauseShapes`). Closed items 3 and the arity red leg. `docs/bugs/resolved/BUG_SUBQUERY_BINDING_ARITY_VALIDATED_AT_DIFFERENT_LAYERS.md`.
9. **CLI optimizer-mode flag** — RESOLVED (2026-07-20), ruled: add the flag, optimize by default. `cmd/datalog` gained `-optimize` (default true; `-optimize=false` = baseline planner), carried on the database's planner options via `openDatabaseOrEDN` so `db.Query`, single-query, and interactive all honor it; the two CLI executor sites now use `db.NewExecutor()` (inheriting options + plan cache). The six subprocess query tests loop the axis via `mode.cliFlag()`; matrix exemption closed in `docs/wip/OPTIMIZER_MODE_MATRIX.md`.

## C. API-surface decisions (July audit — details and Status lines in `docs/reviews/ANTIPATTERN_AUDIT_2026_07.md`)

10. **C1–C4, C7** — RESOLVED (2026-07-20), ruled with role facts after two under-framed first presentations (see the audit entries for the full record):
    - C1 (`UnionRelation` subsystem), C2 (`PrependedRelation`), C3 (`TransformIterator`/`ConcatIterator`) — deleted.
    - C4 (the six algebra methods σ/⋉/▷/extend/γ on `Relation`) — **kept**; the ruling inverted the finding: the engine's free-standing operators are the deviation. Reunification is item 20.
    - C7 — split: `WrapStreamingAsLazy` + `NewLazySeqRelation` kept by ruling (streaming→replayable bridge); the `NoDedupe` pair superseded by `NewMaterializedRelationFromSet` (boundary, admission-checked) + interior `newMaterializedRelationFromSet` (properties-carrying); `Result` alias and `MaterializeResult` deleted.
    - Removed exported API (compatibility notes for the PR): `UnionRelation`/`NewUnionRelation`/`UnionIterator`, `PrependedRelation`/`NewPrependedRelation`/`PrependedIterator`, `TransformIterator`/`NewTransformIterator`, `ConcatIterator`/`NewConcatIterator`, `NewMaterializedRelationNoDedupe`, `NewMaterializedRelationNoDedupeWithOptions` (renamed to `NewMaterializedRelationFromSet`), `MaterializeResult`, `Result`.
11. **C5** — RESOLVED (2026-07-20), executed by ruling. The function namespace has one home: `query.DefaultRegistry` gained implementations (`RegisterImplementation`/`Implementation`, mutex-guarded); `executor.RegisterCustomFunction`/`CallCustomFunction` (the published API) forward to it. Consumption is live in both positions: `FunctionPredicate.Eval` consults the registry (bool result filters; non-bool, unbound-argument, and unregistered names error loudly — the unknown-function message preserved), and expression position parses to the new `query.CustomFunction` when the name has a registered implementation (results normalized via `datalog.NormalizeValue`; unknown names keep the parse-time rejection). Pinned by registry/predicate/expression unit tests (`query/function_registry_test.go`) and the both-modes end-to-end `TestCustomFunctionQueryEndToEnd` (`executor/custom_functions_test.go`). The dead unexported value functions (`addValues` through `extractTimeComponent`) were deleted by owner ruling (2026-07-20, zero callers re-verified); `custom_functions.go` now holds only the two registry forwards, and B2's time-field-switch triplication is fully closed.
12. **C6** — RESOLVED (2026-07-20), ruled: delete. `executor/constraints_impl.go` removed (a parallel constraint home only tests instantiated; production pushdown uses `constraints.TimeRangeConstraint` + the fetch/`ValuesEqual` route). Pins relocated to their semantic homes: canonical comparison asserts directly against `datalog.CompareValues`/`ValuesEqual` (`datalog/compare_canonical_test.go`); the matcher's constraint-filtering test uses a test-local `timeWindowConstraint`. Also removes the third copy of the six-way time-field switch (B2 residual). Full audit record in `docs/reviews/ANTIPATTERN_AUDIT_2026_07.md` C6.
13. **C9** — RESOLVED (2026-07-20), ruled: keep the file and fix the error swallows. The swallows were already fixed in place by the error-swallow sweep (`c6d11d0`, 2026-07-19): `CompareRelations`/`CompareRelationsIgnoreSymbolOrder` return `(bool, error)` with scan errors surfaced, `RelationDiff` and `FormatRelationSummary` include scan failures in their output, and both tuple collectors propagate `Error()`/`Close()`. No code change remained; the ruling closes the keep-vs-delete half.
14. **D7 / E1** — D7 RESOLVED (2026-07-20), ruled and executed: the two remaining un-hoisted per-datom position loops in `matcher_relations.go` converted (`validatingVBoundIterator.buildTuple` deleted in favor of its already-carried `InternedTupleBuilder`, unifying the T representation with the cache paths; `cardinalityManyAVETValueIterator.buildTuple` on a construction-time `query.TupleIndexer`); the `evaluateExpressionWithLookup` bullet was closed earlier by `alignBinding`'s precomputed table; the other two bullets were already settled as recorded. E1 (the two `Materialize()` conventions) remains OPEN, closing via item 20's stage 1 when the reunification arc resumes.

## D. Process calls

15. **Push + PR #112 body update** — RESOLVED (2026-07-20). Pushes landed through `d6d4721`. The PR body was rewritten from scratch by owner ruling: the old body narrated the branch's growth chronologically ("started as… grew… later arcs…") and ended at Class C; the new body states what the PR is — a correctness campaign organized around four commitments (one value domain; one definition per concept; loud failure; optimizer transparency enforced by the dual-mode matrix) — derived from a full read of all 27 branch commit messages, with the compatibility and test sections carried forward and extended through `d6d4721`.
16. **`explain_test.go` gray area** — RESOLVED (2026-07-20), ruled: matrix axis. `TestExplain`, `TestAnalyze`, and `TestAnalyze_ConsumesStreamingResults` (`datalog/storage/explain_test.go`) run under `optimizerModes` via `createOptimizerModeDB`; all legs green — index-selection, bound-mask, and event assertions hold on both modes.
17. **Benchmarks outside the matrix** — CONFIRMED by owner (2026-07-20): the matrix ruling covers tests; benchmarks stay outside it.

21. **Re-re-review findings (at `d6d4721`)** — both RESOLVED (2026-07-20), red-first: the clause-ordering `:in`-bound-correlate hole (first-pick generator preference; the initial all-generators-first version broke the join-project selective-child lowering and was narrowed — `resolved/BUG_ALGEBRA_ORDERING_IN_BOUND_CORRELATE_FOLDS_FIRST.md`) and the predicate-position unknown-function silent empty (`resolved/BUG_PREDICATE_POSITION_UNKNOWN_FUNCTION_SILENT_EMPTY.md`). The three-door extension was ruled and executed same day: the rule's one home is `FunctionPredicate.Validate()` under `query.ValidateStaticClauseShapes` — parser, qb `Build`, and executor entry all reject with one message; the parser constructs unconditionally and defers to the walk; the boundary contract is "registered by the time the query enters the engine", with the per-tuple eval guard as the backstop. Side discovery ledgered under item 7: the lookup-less `missing?` silent empty (open, committed-red).

22. **Third re-review finding + the baseline defect it exposed (at `00fc6e4`)** — both RESOLVED (2026-07-21), red-first, pushed through `dacfdfa`. The reviewer's finding — consumer-only WHERE (`[:find ?e :in $ ?e :where (not [?e :x/flag true])]`) executes on the baseline but errored in the bridge — fixed by seeding `compileClausesFrom` with a childless Project of the environment symbols when the first ordered clause provides nothing (the unit-relation concept from item 5); pinned by `TestConsumerOnlyWhereWithInBoundCorrelates` (not/not-join × both modes). Pinning it exposed a **baseline** defect on both modes: `executePredicate` silently skipped predicates whose required symbols are all constant-resolved scalar `:in` bindings, and nothing rendered constant-bound `:find` symbols into results. Fixed as the completed derivation (`docs/wip/CONSTANT_BOUND_PREDICATE_FIX_DERIVATION.md`, including the two amendments the red baseline forced on it — fail carries as empty groups, not a zero-symbol empty; verdict absorption at the find boundary replaced the falsified single-phase containment assumption): total predicate dispatch (evaluate-once constant arm with entity lookup; loud unprovided-symbols arm — zero hitters in the full suite), zero-symbol unit/empty verdict relations absorbed at the find boundary by the join laws, and `renderConstantFindSymbols` for find variables, pull variables, and aggregate arguments. Reproducers: `TestLeadingMissingWithInBoundEntity` (consumer-only + mixed, both polarities), `TestConstantOnlyPredicateFiltersUniformly`, `TestConstantInputRenderedInFind`. Gate green native + wasm; PR body updated.

23. **`Size()` semantics and sizing economics** — RULED AND DOCUMENTED (2026-07-21). CORE_MODEL Pillar 3's "Size() peek-and-consume" claim did not describe the implementations: no `Relation.Size()` consumes the caller's tuples — each answers free, answers -1, or self-realizes into the pass-through materialized form (spending only an owned upstream source). The economics ruled: stream consumption is the default optimum; realizing for a size pays only when incremental processing is expensive or the realized relation is itself passed through directly afterward; a capacity hint must never be the call that forces work. Call-site behavior left as-was by explicit ruling (the materialized-only pre-allocation guards in `relation_ops.go` stand); any change is a separate discussion. The peek hazard re-aimed at uncaptured iteration. Follow-on ruled and executed (2026-07-21): **`Relation.IsEmpty()` deleted** — bool cannot decline the way `Size()`'s -1 does, so streaming implementations either lied (`StreamingRelation` returned hardcoded `false` in the default mode) or consumed (`OrFallbackRelation` stepped an iterator); the engine carried four "don't call IsEmpty" warnings around it. All seven implementations removed; the three production callers converted — `Size() == 0` at both matcher empty-binding sites (subsuming their `*StreamingRelation` type-guards; the -1 decline is the guard) and consume-then-decide in the table formatter; test assertions converted to exact Size/iteration forms, none weakened. Extended by owner ruling same day ("the entire concept is a virus"): every remaining `IsEmpty` eradicated — `BufferedIterator.IsEmpty()` (zero production callers), `query.Relation.IsEmpty()` (zero callers), `OrderedSet.IsEmpty()` (tests converted to `Len()`), the last stale don't-call comments, and the live interface listings in ARCHITECTURE.md / RELATIONAL_ALGEBRA_OVERVIEW.md / ARCHITECTURAL_PHILOSOPHY.md (whose mistake table also carried the false "Size() forces materialization, may lose data" row — corrected). Zero `IsEmpty` identifiers remain in Go code; historical narratives (TALE, bugs/resolved, papers, archive) left as records. Gate green native + wasm.

## Added since the ledger was first drawn

18. **Correctness-measurement hierarchy** — PROPOSED (2026-07-20), pending ruling. Oracle tiers by independence; staged S1 clause-order invariance (also the property form of item 2) → reference evaluator + three-way differential → grammar generator → metamorphic suites. `docs/wip/CORRECTNESS_MEASUREMENT.md`.
19. **Wasm sibling for `default_store_test`** — RESOLVED (2026-07-20), ruled and executed: `datalog/storage/default_store_wasm_test.go` / `TestOpenDefaultStoreUsesMemory` (build-tagged `js && wasm`) asserts `openDefaultStore` returns `*MemoryStore`, mirroring the native Badger assertion. Green under js/wasm.

20. **Relation-algebra reunification arc (the C4 ruling's execution)** — DESIGN REWORKED AND PARTIALLY RATIFIED (2026-07-22); implementation still queued behind correctness work. The parked design at `docs/wip/RELATION_ALGEBRA_REUNIFICATION.md` was reworked against the environment-relation and set-semantics-at-birth arcs, with two owner rulings: (a) **`Replayable()` rejected** — reuse is a static property of the consuming algorithm (no consumer has ever not known it needs to reuse a relation), and interface declarations carry knowledge from the side that has it to the side that lacks it (`RequiresCopy()` passes that test; a replay predicate flows backwards, the deleted-`IsEmpty()` shape); consumption is requested through `Materialize()`'s sharpened E1 contract (lazy lift, self-when-shareable), pinned behaviorally — stage 1 closes item 14's E1 with no interface change; (b) **clauses close over the environment *relation*** — at evaluation sites the environment is consulted, not joined (Available, never Provides), which is lexical scope, and its formalization is closure: `ClosePredicate`/`CloseExpression` live in the executor (the capture is the executor's `Relation` type; closed types implement the `query` interfaces), superseding the original draft's `query`-package map capture — the carrier the campaign retired. Closure gives "a clause evaluates under its scope's environment" one enforcement point (the algebra lowering's forgot-to-thread `not`-leg bug becomes unrepresentable) and is the only shape under which the interface methods absorb the database-function family so the free operators can be deleted. Joins remain joins where environment symbols enter schemas (branch correlation, `notBodyBinding`, find rendering). Remaining open sub-decisions: `Union` error shape; `EvaluateFunction` removal compatibility bundling. Stage-2 benchmark re-gate required against the environment-arc carrier-swap result.

Ruled and queued (not pending decisions): the collection-aggregate substrate `(min n ?x)`/`(max n ?x)`/`(distinct ?x)`, result-side collection binding `(q ...) [?coll ...]` (one embedded semantic sub-choice: empty result drops the outer tuple vs binds `[]`), `count-distinct`, then subquery `:limit`/`:order-by` — see `TODO.md` Medium Term.

24. **The environment is a Relation (or-join `:in`-bound correlates)** — RULED AND STAGE 1 EXECUTED (2026-07-22). Rulings: `:in` scalar/tuple parameters are the query scope's environment — ambient in every clause scope, never branch locals, never alpha-renamed; the carrier is **one single-tuple Relation** on the executor `Context` (`Environment()`/`WithEnvironment`), bound at every query-scope boundary and reaching consumers **by join**, never as a `map[Symbol]interface{}` (the carrier the 2025-06-26 Bindings→Relations migration removed and `599266f`'s `constantBindings` reintroduced); no scalar shadows of the environment (consumers read `Symbols()`/`Get(0)` from the relation); single-consumer derived state is a closure capture, not struct surface; or-branch visibility derives from the canon (`query.ScopeOf` Provides ∪ Correlates for explicit-header forms, branch free variables for inference forms) — the executor's parallel derivations (`headerSyms`, `collectOrBranchRequiredSymbols`) deleted. Two commits ruled: stage 1 (the fix, landed with mode-matrixed reproducers in `datalog/storage/orjoin_in_bound_correlate_test.go`, all-env residual-group absorption at the find boundary, and the `clausesNeedCorrelation` NOT-taxonomy fix) and stage 2 (retiring the `constantBindings` map channel: `filterWithPredicateAndLookup`/`evaluateExpressionWithLookup`/`thetaJoinWithPredicate`/`thetaJoinPair`/`crossJoinWithExpression` take the environment Relation and bind its row by reference per evaluation; `evalPredicateOnce` deleted — env-only predicates filter the environment's single tuple, symbol-free predicates the zero-symbol unit; `renderConstantFindSymbols` is `group.Join(env.Project(missing))`; the stage-1 interim `environmentBindings` conversion deleted; checkpoint benchstat wall −3.4% p=0.002, B/op and allocs flat) — stage 2 EXECUTED. Follow-ups ruled: subquery `inputValues`/`outerValues` maps convert in their own arc (still pending); `BUG_NOT_SPANNING_DISJOINT_GROUPS_APPLIED_PER_GROUP` got its own arc — RESOLVED (2026-07-22, the anti-join bridging commit): `executeNotClause`/`executeNotJoinClause` select the clause's subject via `findOuterRelation` (spanning groups join once — the clause's correlation is the connector, the anti-join analog of the bridging theta-join; keyless groups pass through), anti-join once on the full key set, and `replaceGroups` (extracted from the or-fallback's replacement) substitutes the filtered join; `notBodyBinding` renders the environment's row into the body binding — the `not` body scope boundary treats the environment like every other, which also fixes the algebra lowering's narrow-header form. Pinned by `datalog/storage/not_spanning_groups_test.go` (bare `not`, `not-join` header, `:in`-bound correlate — all mode-matrixed) and the formerly ruled-red `TestOrJoinBranchNotConsumesWhereBoundExternal`, now green: the branch carries zero red tests. Fix doc: `BUG_ORJOIN_IN_BOUND_CORRELATE_TREATED_AS_BRANCH_LOCAL`.

25. **Correct-by-construction plans, with Datalog as the IR** — RAISED by owner (2026-07-22); DERIVATION DOCUMENTED AND EMPIRICALLY VERIFIED (2026-07-22), pending ratification and scheduling at `docs/wip/CORRECT_BY_CONSTRUCTION_PLANS.md`. The observation, from the anti-join bridging arc: the executor's after-planning machinery — per-clause subject discovery (`findOuterRelation`, the `relevantRels` partitions), runtime bridging (theta-join for predicates, anti-join bridging for `not`), loud unbound checks, find-boundary absorption, and the strict phase contracts — exists because RealizedPhases specify symbol flow but not group topology; the clause loop improvises topology at runtime, and this campaign's bugs clustered in exactly that improvised layer. The algebra path constructs the topology explicitly and then discards it at decompile — the sound computation is the one thrown away. **The owner's position that the IR remains expressible as Datalog is verified, not assumed**: the counter-allegation (that the language can't carry group topology) was falsified against the tree-built CLI on both planner modes — shared-symbol multi-input unification executes as the join, disjoint inputs under a spanning predicate execute as the theta-join, and a spanning `not-join` over two relation inputs executes as the bridged anti-join. The construction: a plan is a DAG of query fragments whose `:in` lists name the specific prior outputs each fragment consumes — subject selection, bridging, and absorption become declared fragment properties; strict contracts become one plan-construction validation; fragment granularity is the planner's dial (declare topology at compound-clause scope boundaries, keep greedy Collapse fragment-internal between them, deferring the statistics dependency). Open for ratification: granularity policy, the dual-mode matrix's role afterward, sequencing behind item 20 (fragments consume the reunified operator surface). Incidental defect found during verification, at the CLI layer: `BUG_CLI_RELATION_INPUT_EDN_REJECTED` (open).

26. **Algebra provenance as values (`ExplainAlgebra`)** — RULED AND EXECUTED (2026-07-22). The gap: downstream applications had no surface for "what is the algebra of this query" or "what transforms happened" — the optimized tree died inside the bridge at decompile, `Database.Explain` returned the physical plan only with a nil handler, and transform provenance existed as annotation crumbs from one of two passes (the get-else rewrite fired invisibly). Ruled: provenance returns **as values, never by event scraping** (per the annotations-are-never-a-data-channel rule), with events remaining the streaming view derived from the same call sites; every pass and bridge transform reports uniformly. Executed: `algebra.RewriteRecord`/`RewriteSink` (records the source of truth; `Collect` gates accumulation so the normal query path pays nothing); pass constructors take the sink (`DecorrelationPass`, `GetElseScanRewritePass`, `DefaultPasses` — exported signature changes; `Optimizer.WithHandler` and its by-name pass-rebuild deleted, which also closes the hazard of a new pass silently losing its handler); the get-else pass and the bridge's join-project insertion gain full provenance (new events `algebra/getelse-scan-{apply,skip}`, `algebra/join-project-{apply,skip}`); `planner.AlgebraExplanation` + `QueryPlanner.ExplainPlan` (cache-bypassing in both directions); `Database.ExplainAlgebra(queryInput, inputs...)` on the public surface (`db.DB` alias), returning original, compiled tree, rewrite records, optimized tree, rewritten Datalog, and the physical plan — with the optimizer disabled the compiled algebra still returns (the algebra of a query is a fact about the query) alongside the baseline plan. CLI wiring (owner-directed, same arc): `cmd/datalog -plan-only` plans without executing and prints the explanation, composing with `-optimize` and `-in`; `-plan-only` without `-query` errors. Pinned by `algebra/provenance_test.go` (sink contract, both passes' applied/declined records with reasons, event forms), `storage/explain_algebra_test.go` (end-to-end both optimizer modes, decline reasons, input validation, rendering), and `TestCLI_PlanOnly` (subprocess, explicit per-mode structure pins, no-execution assertion, requires-query error). Compatibility notes for the PR: the three pass-constructor signatures, `Optimizer.WithHandler` removal, `QueryPlanner` interface gains `ExplainPlan`.

27. **not/not-join binding domains include the environment; freeness is the algebra's environment representation** — RULED AND EXECUTED (2026-07-22). The ruling (ACCEPT canonical, four grounds — Datomic compatibility strongest, inference-form coherence, or-join symmetry, vacuous-truth declaration): an `:in`-bound symbol is an ordinary bound variable in every not/not-join boundness judgment — declaring one in a not-join header is valid, and a body consuming one only through a predicate is valid with the header declaring it. Derivation, empirical six-shape × two-mode matrix, and the four-home class fix at `docs/wip/NOTJOIN_HEADER_ENV_BINDING_DERIVATION.md`: `boundDomain` reified once in the algebra compiler (`compileNotJoin`'s three arms, `compileNot`); `analyzeAntiJoin`'s left-child-production errors became free-requirement propagation (uniform with Select/Map/Union — **`Analyze` must not learn about inputs**: compile is value-free because plans cache across input values, so freeness at the root *is* the query's environment demand); `filterWithNotJoinClause` classifies header variables (subject-carried → key; environment-bound → body constraint via `notBodyBinding`, vacuous in the key; neither → loud error — never the silent-existential intersection). Second divergence found and fixed in the same arc: bare `not` with predicate-only env consumption (`compileNot`). Shape 4 (omitted header, predicate-only) stays a parse error by the header-declaration rule — mode-uniform, now pinned for the first time. Pinned by ten mode-matrixed tests in `not_spanning_groups_test.go` (three former reds, two stay-greens, the parser pin, two bound-nowhere negative controls, three iterated-input legs), three `analyzeAntiJoin` structural units, and a two-shape `ExplainAlgebra` structure pin. **Amended after the round-4 review**: over-declaration is rejected on both modes — a header symbol the body neither produces nor consumes declares a correlation that does not exist (declared ⇔ produced-or-consumed; or-join symmetry decisive), executor-only fix mirroring the compile arm's message, two further mode-matrixed pins covering the `:in`-bound shape (the review's find) and the WHERE-bound sibling that had diverged since before the campaign.

28. **Expression results resolve at one admission boundary** — RULED AND EXECUTED (2026-07-22). `admitExpressionResult` — already called at every evaluation site — consumes the get-some absence sentinel and validates the resolved value, returning `(value, found, error)`; the signature change makes site-completeness compiler-enforced, replacing four hand-rolled unwraps and closing two sites that leaked the raw `*query.GetSomeResult` into tuples (panic at tuple-key hashing): the all-constants environment arm (both `Found` states) and the zero-required-symbols arm (get-some over a tagged-literal entity — a sixth site no report had caught, probe-confirmed). Absence semantics per site: per-tuple operators skip the tuple; no-groups and literal-entity arms return empty; the with-groups arm empties every group schema-preservingly (the phase contract's Provides survives). The interface-change alternative (Eval returning `(interface{}, bool, error)`) was considered and declined: it rebuilds every Function implementation for one absence-capable function; the boundary achieves the same single-homed invariant. Fix doc: `BUG_GETSOME_ALL_ATTRS_MISSING_PANICS_TUPLE_KEY_HASH` (resolved).
